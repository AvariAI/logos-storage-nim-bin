--- a/vendor/nim-leveldbstatic/leveldbstatic/prelude.nim
+++ b/vendor/nim-leveldbstatic/leveldbstatic/prelude.nim
@@ -16,7 +16,10 @@ proc buildLevelDb() =
building LevelDB: " & cmd
   let (output, exitCode) = gorgeEx cmd
   if exitCode != 0:
     discard gorge "rm -rf " & buildDir
-    raise (ref Defect)(msg: "Failed to build LevelDB")
+    when defined(CLIENT_LITE):
+      echo "CLIENT_LITE: Skipping LevelDB build failure"
+    else:
+      raise (ref Defect)(msg: "Failed to build LevelDB")

 static:
+  when not defined(CLIENT_LITE):
   buildLevelDb()