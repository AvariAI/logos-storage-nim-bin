diff --git a/vendor/logos-storage-nim-dht/codexdht.nimble b/vendor/logos-storage-nim-dht/codexdht.nimble
index 1234567..abcdef8 100644
--- a/vendor/logos-storage-nim-dht/codexdht.nimble
+++ b/vendor/logos-storage-nim-dht/codexdht.nimble
@@ -11,12 +11,17 @@ skipDirs      = @["tests"]
 
 # Dependencies
 requires "nim >= 2.2.4 & < 3.0.0"
 when defined(CLIENT_LITE):
+  # Client-lite mode - mobile friendly dependencies only
   requires "secp256k1 >= 0.6.0 & < 0.7.0"
   requires "nimcrypto >= 0.6.2 & < 0.8.0"
   requires "bearssl >= 0.2.5 & < 0.3.0"
   requires "chronicles >= 0.11.2 & < 0.13.0"
   requires "chronos >= 4.0.4 & < 4.1.0"
   requires "libp2p >= 1.14.1 & < 2.0.0"
   requires "metrics >= 0.1.0 & < 0.2.0"
   requires "stew >= 0.4.2"
   requires "stint >= 0.8.1 & < 0.9.0"
   requires "https://github.com/logos-storage/nim-datastore >= 0.2.1 & < 0.3.0"
   requires "questionable >= 0.10.15 & < 0.11.0"
+else:
+  # Full mode - includes LevelDB
+  requires "nim >= 2.2.4 & < 3.0.0"
+  requires "secp256k1 >= 0.6.0 & < 0.7.0"
+  requires "nimcrypto >= 0.6.2 & < 0.8.0"
+  requires "bearssl >= 0.2.5 & < 0.3.0"
+  requires "chronicles >= 0.11.2 & < 0.13.0"
+  requires "chronos >= 4.0.4 & < 4.1.0"
+  requires "libp2p >= 1.14.1 & < 2.0.0"
+  requires "metrics >= 0.1.0 & < 0.2.0"
+  requires "stew >= 0.4.2"
+  requires "stint >= 0.8.1 & < 0.9.0"
+  requires "https://github.com/logos-storage/nim-datastore >= 0.2.1 & < 0.3.0"
+  requires "questionable >= 0.10.15 & < 0.11.0"
+  requires "leveldbstatic >= 0.2.1 & < 0.3.0"